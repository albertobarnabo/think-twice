---
description: "Enforce strict scope on a task — build only what was explicitly asked, nothing more"
argument-hint: "the task to implement with strict scope"
---

Implement the following task under strict surgical rules:
build exactly what is asked, nothing more.
No unrequested error handling, abstractions, tests, refactors, or future-proofing.
After completing, note in one line what was deliberately left out.

Task: $ARGUMENTS
